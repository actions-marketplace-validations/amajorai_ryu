// The modal suite does not exercise the agent composer or its host services.
// Keep RoomWorkbench and its API/error behavior real while isolating that sibling.
export function InputBar() {
	return null;
}
