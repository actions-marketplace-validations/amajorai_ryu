[package]
name = "ryu-safe-actions"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Pure typed-plan protocol and deterministic verifier for Ryu Safe Actions."
repository = "https://github.com/amajorai/ryu"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
