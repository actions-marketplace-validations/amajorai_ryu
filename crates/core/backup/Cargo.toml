[package]
name = "ryu-backup"
version = "0.4.0"
edition = "2021"
license = "Apache-2.0"
description = "S3-compatible encrypted backup transport and consistent filesystem snapshots for Ryu. No dependency on Core or app satellites."

[dependencies]
anyhow = "1"
aes-gcm = "0.10"
base64 = "0.22"
chrono = { version = "0.4", features = ["serde"] }
futures-util = "0.3"
hex = "0.4"
hkdf = "0.12"
object_store = { version = "0.12.4", default-features = false, features = ["aws", "tls-webpki-roots"] }
rand = "0.8"
reqwest = { version = "0.12", default-features = false, features = ["rustls-tls", "stream"] }
rusqlite = { version = "0.40.0", features = ["bundled", "backup"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
tempfile = "3"
tokio = { version = "1", features = ["fs", "io-util", "rt", "macros", "sync"] }
url = "2"
utoipa = { version = "5", features = ["chrono"] }
uuid = { version = "1", features = ["v4"] }
zip = "0.6"

[dev-dependencies]
axum = "0.7"
